import * as THREE from 'three';

export const freeLookOrigin = { position: new THREE.Vector3(0, 1, 3.1), fov: 50 };

export const focusConfigs = { mac: { position: [0, 1.023, 2.68], lookAt: [0, 1.023, 2] } };
